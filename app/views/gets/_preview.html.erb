<%= form_for(@get, remote: true) do |f| %>

  <div class="field">
    <%= f.label t(:url) %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit "getData_ajax" %>
  </div>
<% end %>
