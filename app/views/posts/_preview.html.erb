<%= form_for(@post, remote: true) do |f| %>

  <div class="field">
    <%= f.label t(:url) %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit "test" %>
  </div>
<% end %>
